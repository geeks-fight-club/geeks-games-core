//
//
//

const mongoose = require('mongoose');
const Schema = mongoose.Schema;
const ObjectId = Schema.Types.ObjectId;
mongoose.Promise = global.Promise;

let players_schema = Schema({

  username: String,
  name: String,
  email: String,
  uuid: String,

  _disable: Boolean
});


module.exports = mongoose.model('players', players_schema);
