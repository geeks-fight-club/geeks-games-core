//
//
//

function clock_pulse(interval, cb) {

  cb = cb || function() {};

  if (!interval) {
    return cb({
      status: 400,
      err: new Error('low args')
    });
  }

  setInterval(function() {
    cb();
  }, interval);

}

module.exports = clock_pulse;
